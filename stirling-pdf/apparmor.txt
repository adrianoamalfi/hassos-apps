#include <tunables/global>

profile stirling_pdf flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Receive signals from S6
  signal (receive) peer=*_supervisor,

  # Access to options.json and other Home Assistant files
  /data/** rw,

  # Access to mapped addon_config
  /config/** rw,

  # Application files
  /opt/stirling-pdf/** r,
  /opt/stirling-pdf/stirling-pdf.jar r,

  # Java runtime
  /usr/lib/jvm/** mr,
  /usr/bin/java ix,

  # LibreOffice
  /usr/lib/libreoffice/** mr,

  # Temp files
  /tmp/** rwk,

  # Proc filesystem
  /proc/sys/vm/overcommit_memory r,

  # Network access
  network inet stream,
  network inet dgram,
  network inet6 stream,
  network inet6 dgram,
}
