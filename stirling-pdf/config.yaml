---
name: Stirling PDF
version: "1.0.0"
slug: stirling_pdf
description: A self-hosted PDF manipulation tool with merge, split, convert, and more.
url: https://github.com/adrianoamalfi/hassos-apps/tree/main/stirling-pdf
arch:
  - amd64
  - aarch64
startup: services
init: false
ingress: true
ingress_port: 8080
panel_icon: mdi:file-pdf-box
panel_title: Stirling PDF
ports:
  8080/tcp: null
ports_description:
  8080/tcp: "Web UI (disabled by default, use Ingress)"
map:
  - addon_config:rw
options:
  log_level: info
schema:
  log_level: list(trace|debug|info|warning|error|fatal)
image: ghcr.io/adrianoamalfi/{arch}-addon-stirling-pdf
