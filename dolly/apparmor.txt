#include <tunables/global>

profile dolly flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Receive signals from S6
  signal (receive) peer=*_supervisor,

  # Access to data directory
  /data/** rw,

  # Access to mapped addon_config
  /config/** rw,

  # Application files
  /opt/dolly/** rw,

  # Python runtime
  /usr/bin/python3* ix,
  /usr/lib/python3*/** r,

  # Temp files
  /tmp/** rwk,

  # Network access
  network inet stream,
  network inet dgram,
  network inet6 stream,
  network inet6 dgram,
}
